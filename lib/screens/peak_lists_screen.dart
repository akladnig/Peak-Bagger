import 'package:flutter/material.dart';

class PeakListsScreen extends StatelessWidget {
  const PeakListsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Peak Lists')));
  }
}
