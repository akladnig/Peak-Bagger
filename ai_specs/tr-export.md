# Track and Route export
## Goal
Adds an export button to the track/route side drawer

- Placed at the bottom of the drawer
- Tracks are saved by default to ~/Downloads
- Routes are saved by default to ~/Documents/Bushwalking/routes 

The routes gpxRouteJson will need to be converted to a valid gpx route file.
