# On Screen Track Selection
## Goal
Extend the track detection feature to allow the track hovered over to be selected on a mouse click.
The selected track should then be highlighted by changing the track colour to green.
