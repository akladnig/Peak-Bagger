# Phase 4a
Adds ability to go to a specific map using the goto location feature.

- The search box adds the ability to enter a map name only, with no grid references. In this case navigate to the center of the map.
- 
- Once centred on the map, zoom to the map extents, and draw a blue rectangle around the map extents so that the map can be seen when zooming out.
- add a padding around the map so that the blue outline is visible.

# Phase 4b
Implement once phase 4a is complete
- Adds a show maps icon which is a blue grid of all maps.
- Use Material Icons GridOn
- Position in between goto location and information fabs
- For each map, draw a blue rectangle around each map and show the map name and series at the bottom right of each map.

