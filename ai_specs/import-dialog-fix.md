# Import Dialog Fix
Fix UI issues with the import dialog

Refer to dialog.png
Update the dialog to have the same padding/margin as the Peak Info Popup
"Import GPX File(s)" should be on a single line with no wrap
If more than 1 file is selected for import the dialog should expand vertically to display all selected files up to the maximum height of the screen less a top and bottom padding, after which verical scrolling should be enabled.

If a duplicate track is attempted to be imported surface an message: "Duplicate track not imported"
