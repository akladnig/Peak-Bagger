# Show Tracks FAB update
# Goal
Update the functionality of Show Tracks to allow the display of routes, tracks, both or none.

Rename "Show Tracks" to "Show Tracks/Routes (T)"
- On click of Show Tracks display a drawer as per Basemaps or Select Peak List.
- Show the following two items with a toggle switch at the start:
  - Show Tracks
  - Show Routes

- Persist the state of the switches across app restarts in shared prefs.
