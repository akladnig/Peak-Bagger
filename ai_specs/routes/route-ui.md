# Route UI updates
Updates the UI for Route Creation

- Create 2x separate sheets/popups: 1 for the elevation graph display (see elevation.png) and a second for the buttons (buttons.png).
- Both popups/sheets should have the same padding 
- The buttons popup/sheet should be minimised vertically so that it is a thin strip and aligned to the bottom right of the screen
- The elevation graph should be aligned to the bottom left.
- Vertically align the Route Name Text Box, Cancel and Save so that they are aligned with all the remaining buttons and FABs
- 
