# Add functionality to the import track FAB on the map screen
## Goal
On clicking the import track FAB a file selector dialog will be displayed allowing selection of one or more gpx tracks from the default track folder.

The import functionality should be as per reset track data, but no track is to be deleted, only added.

- use the same dialog as per the Peak List import Peak List, allowing the user to change the base name (but not the date) of the track.

### files to examine
- examine all ai_specs/005.*spec.md and  ai_specs/005.*plan.md files.
